name: wizlight
description: A Dart package for controlling WiZ smart light bulbs over UDP using JSON protocol. Port of wizlightcpp with support for discovery, color control, brightness, scenes, and device management.
version: 1.0.0
homepage: https://github.com/sjhorn/wizlight
repository: https://github.com/sjhorn/wizlight

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.4.2
  logging: ^1.2.0

dev_dependencies:
  test: ^1.25.0
  lints: ^3.0.0
